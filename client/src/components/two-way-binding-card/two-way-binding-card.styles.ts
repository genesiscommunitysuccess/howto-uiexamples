import { css } from '@genesislcap/web-core';

export const TwoWayBindingCardStyles = css`
  :host {
    padding: calc(var(--design-unit) * 0.4rem);
    width: calc(var(--design-unit) * 2.5rem);
    border: calc(var(--design-unit) * 0.5px) solid var(--neutral-foreground-rest);
    border-radius: calc(var(--design-unit) * 2px);
  }
`;
